= simple_form_for([:admin, @page, @sub_page]) do |f|
	= f.input :title
	= f.input :content
	= f.input :publish, :as => :boolean
	= f.input :page_id, as: :hidden, :input_html => {:value => @page.id}
	= f.button :submit, :class => 'btn-primary'